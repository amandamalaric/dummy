testFleet:
    handler: handler.getSKU
    events:
      - iotFleetProvisioning:
        templateBody: ${file(template.json)}
        provisioningRoleArn: !Sub 'arn:aws:iam::${AWS::AccountId}:role/IoTProvisionByClaimRole'